import React from 'react';

const Board = () => {
  const boardStyle = {
    width: '200px',
    height: '100px',
    backgroundColor: 'blue',
  };

  return (
    <div style={boardStyle}>
      {/* Content goes here */}
    </div>
  );
};

export default Board;
